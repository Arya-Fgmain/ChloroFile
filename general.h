// libraries generally needed are all put into one file
#if !defined(USUALS_H)
#define USUALS_H

// standard library headers

#include <algorithm>
#include <chrono>
#include <cstdint>
#include <deque>
#include <iostream>
#include <memory>
#include <mutex>
#include <thread>
#include <vector>

#endif